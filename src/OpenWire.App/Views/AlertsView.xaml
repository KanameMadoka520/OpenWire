<UserControl x:Class="OpenWire.App.Views.AlertsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:OpenWire.App.ViewModels"
             d:DataContext="{d:DesignInstance vm:AlertsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
    <UserControl.Resources>
        <CollectionViewSource x:Key="Grouped" Source="{Binding Alerts}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Time" Converter="{StaticResource DayGroup}" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
    </UserControl.Resources>

    <Grid Margin="24,18,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Alerts" Style="{StaticResource H1}" Margin="0,0,0,12" />

        <Grid Grid.Row="1" Margin="0,0,0,4">
            <StackPanel Orientation="Horizontal" Background="{StaticResource BgSubtle}" HorizontalAlignment="Left">
                <StackPanel.Resources>
                    <Style TargetType="RadioButton" BasedOn="{StaticResource Seg}"><Setter Property="GroupName" Value="sev" /></Style>
                </StackPanel.Resources>
                <RadioButton IsChecked="{Binding SeverityFilter, Converter={StaticResource EnumMatch}, ConverterParameter=All, Mode=TwoWay}">
                    <TextBlock><Run Text="All " /><Run Text="{Binding AllCount, Mode=OneWay}" Foreground="{StaticResource TextMuted}" /></TextBlock>
                </RadioButton>
                <RadioButton IsChecked="{Binding SeverityFilter, Converter={StaticResource EnumMatch}, ConverterParameter=Important, Mode=TwoWay}">
                    <TextBlock><Run Text="Important " /><Run Text="{Binding ImportantCount, Mode=OneWay}" Foreground="{StaticResource TextMuted}" /></TextBlock>
                </RadioButton>
                <RadioButton IsChecked="{Binding SeverityFilter, Converter={StaticResource EnumMatch}, ConverterParameter=Log, Mode=TwoWay}">
                    <TextBlock><Run Text="Log " /><Run Text="{Binding LogCount, Mode=OneWay}" Foreground="{StaticResource TextMuted}" /></TextBlock>
                </RadioButton>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Grid Width="240" Margin="0,0,10,0">
                    <TextBox Style="{StaticResource Search}" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Text="Search" Foreground="{StaticResource TextMuted}" FontSize="13" Margin="12,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"
                               Visibility="{Binding SearchText.Length, Converter={StaticResource ZeroVis}, ConverterParameter=invert}" />
                </Grid>
                <Button Content="Mark all read" Style="{StaticResource BtnGhost}" Command="{Binding MarkAllReadCommand}" />
            </StackPanel>
        </Grid>

        <TextBlock Grid.Row="2" Text="{Binding EmptyText}" Style="{StaticResource Caption}"
                   HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" MaxWidth="420" TextAlignment="Center"
                   Visibility="{Binding Alerts.Count, Converter={StaticResource ZeroVis}, ConverterParameter=invert}" />

        <ListBox Grid.Row="2" Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource PlainRow}"
                 ItemsSource="{Binding Source={StaticResource Grouped}}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,8,0,0">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" Style="{StaticResource TableHeaderText}" Margin="4,16,0,6" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource BgPanel}" BorderBrush="{StaticResource Border}" BorderThickness="1" CornerRadius="8" Padding="14,12" Margin="0,0,0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Background="{StaticResource NewBadge}" CornerRadius="10" Padding="7,2" Height="20" VerticalAlignment="Top" Margin="0,1,12,0"
                                    Visibility="{Binding Acknowledged, Converter={StaticResource BoolVis}, ConverterParameter=invert}">
                                <TextBlock Text="NEW" Foreground="White" FontSize="10" FontWeight="Bold" VerticalAlignment="Center" />
                            </Border>
                            <Image Grid.Column="1" Width="22" Height="22" VerticalAlignment="Top" Margin="0,0,12,0" Source="{Binding AppId, Converter={StaticResource AppIcon}}" />
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Message}" Foreground="{StaticResource TextSecondary}" FontSize="12.5" TextWrapping="Wrap" Margin="0,3,0,0" />
                            </StackPanel>
                            <TextBlock Grid.Column="3" Style="{StaticResource Mono}" FontSize="12" Foreground="{StaticResource TextMuted}" VerticalAlignment="Top" Margin="12,0,0,0"
                                       Text="{Binding Time, Converter={StaticResource LocalTime}, ConverterParameter='HH:mm'}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
